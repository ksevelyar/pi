* ask questions if unsure
* use nix run to run commands, like `nix run nixpkgs#python3`
* economy of complexity is the main goal
* don't use semicolons for js or ts, don't use tabs, use spaces
* don't add comments, use semantic names instead
