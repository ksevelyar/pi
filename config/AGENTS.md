* ask questions if unsure
* use nix run to run commands, like `nix run nixpkgs#python3`
* clarity and economy of complexity are important
* don't use abbreviations, don't add new comments for both cases use semantic names, like extracting operations to private function with semantic name
* avoid redundancy in namespaces
